'use strict';

var Keystore = require('./keystore');
var Keygen = require('./keygen');

var ecc = require('eosjs-ecc');

module.exports = {
  Keystore: Keystore,
  Keygen: Keygen,
  modules: {
    ecc: ecc
  }
};